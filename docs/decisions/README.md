# Decision records

One decision per file, `YYYY-MM-DD-<kebab-slug>.md`, newest first. Never edit a
past decision to change its meaning — supersede it with a new dated file and
cross-link both directions.

**Check this index before proposing a change.** A settled decision overrides
default instinct.

| Date | Decision |
| --- | --- |
| 2026-08-27 | [A leftover control refuses a live rip, and the live set has one answer](2026-08-27-a-leftover-control-refuses-a-live-rip.md) — the owner: *"Fix everything now before I start ripping again. I don't wanna wind up in a bad situation."* ⚠️ **The leftovers panel could DELETE a rip that was still running, and after PR #23 could rename one too.** `.rip-deck-incomplete-<uuid>` is where a LIVE rip writes, from `prepareDestination` until the atomic rename at the end, and on nine drives there can be nine filling up at once — so an in-flight rip was listed as a deletable leftover with its bytes still arriving. `reaper.ts` has guarded exactly this since it was written and names it in its header; `refusalToDeleteLeftover` had four rules and not one asked whether a job claimed the uuid. The fifth rule now lives in the helper BOTH verbs share, so a third verb inherits it, and `liveRips` is a required argument with no default — a forgotten argument is a type error, not a deleted rip. **One answer to "is this rip live"**, in `rip/liveRips.ts`: the watcher's bay table (`starting` or `ripping` — the phase filter is the whole function, because `BayState.jobUuid` SURVIVES the outcome latch) unioned with `reaper.ts`'s own `/proc` argv reader, which is the evidence that survives a daemon restart. Unknown is a STATE, not an empty set: `{ isKnown: false, reason }` fails closed, because "no job claims this uuid" and "we could not find out" must never read as the same thing. A duplicate landing is exempt — its marker is written by the rename that ENDS a rip. ⚠️ **An in-flight row is LISTED AND LOCKED, never hidden**: a hidden row makes the panel disagree with the bay grid about what is on disk, and an operator who cannot see the folder cannot tell "no leftover" from "the panel is not showing one". `is_locked` disables Rename AND Delete and prints the reason, because a refusal met by pressing the button is worse than a disabled button. **Refines** the same day's rename decision |
| 2026-08-27 | [Rip history is an append-only log beside the job files](2026-08-27-rip-history-is-an-append-only-log-beside-the-job-files.md) — the owner: *"there's no way to view rips that were previously done since the last time the image restarted … I'd like a way to also see older ones, check by date, etc."* The cause is not the restart: `bays.json` holds ONE record per bay and overwrites it, so a rip is unfindable the moment the next disc lands in that tray. Every terminal outcome now appends one line to `$RIP_DECK_STATE_DIR/history.jsonl` at the latch — the only instant where the disc's name, its type, its destination and the outcome are all in hand — and `GET /api/history` filters, sorts and SLICES before joining each returned row to its `<uuid>.features.json` and `<uuid>.verdict.json`. ⚠️ The join is per PAGE: enriching the whole log would be two file opens per rip ever done, which is what `?limit=`'s cap of 200 bounds. Nothing is pruned; a row is a few hundred bytes. The 26 rips already on the tower are rebuilt at boot, keyed on job id so a live row always wins — and those rows have **no disc name and none can be recovered**: measured on the real 27-capture corpus, the name is absent from every `DRV:` line (`backup` never prints it), `MSG:5072` names only the `.rip-deck-incomplete-<uuid>` temporary, and the closest rip-folder mtime was over a DAY out on all 13 successful jobs. `is_named` keeps "nobody recorded one" apart from "the disc had no label". The verdict passes the same `isHealthVerdictPublished()` + `hedged()` gate the bay card passes. ⚠️ The page is ONE COLUMN — a title, a date and a sentence is a reading surface, per the 2026-08-25 narrowing; `BayGrid` stays a grid |
| 2026-08-27 | [A read error on a verified backup is a WARNING, not a failure — and the CSS handshake probe is not a read error at all](2026-08-27-a-read-error-on-a-verified-backup-is-a-warning-not-a-failure.md) — slot 1 rode to `MSG:5070`/`MSG:5081 "Backup done"`, left an 8,070,922,240-byte mountable ISO, and was badged **fail**. The whole evidence was ONE `MSG:2003` at offset 1 MB, 28 lines in and **before** `MSG:5072` — MakeMKV probing a CSS DVD before `mmgplsrv` supplies the key, which every protected DVD produces and which is supposed to fail. It is now dropped, on **both** halves of the test (position AND the `READ OF SCRAMBLED SECTOR` sense), so a mid-disc scrambled error still counts and a pre-backup error with another sense still counts. A GENUINE read error on a backup that verified is the third state: `isRipSuccessful` no longer opens `if (readErrorCount !== 0) return false`, `BayOutcomeKind` gains `completed_with_warnings`, and the rail chip reads `warning` in amber on the calm surface — distinct from the finished green, the failed red, and a HELD bay's filled amber. The owner: *"It didn't fail because it made the ISO, but the ISO is problematic, so I'd like to know that."* ⚠️ **Robot mode cannot say whether MakeMKV recovered** — the 1.18.4 catalogue was read end to end and carries no retry/recovery/zero-fill message, so the warning STATES that gap rather than implying an answer; `MSG:5085`'s hash-table verification is the one real integrity signal and is Blu-ray only. Also wires `MSG:5069`/`MSG:5080` "Backup failed", whose `hasFailureMessage` input had had no producer since the function was written, and stops throwing away `verifyBackupStructure`'s own sentence — three different situations were all arriving as the bare word `empty_output`. **Refines** the 2026-08-26 chip decision (a failed bay stays red) |
| 2026-08-27 | [A leftover rename refuses rather than clobbers, and keeps the `.iso`](2026-08-27-a-leftover-rename-refuses-rather-than-clobbers.md) — the owner, on a Teenage Mutant Ninja Turtles box set whose UDF volume labels are wrong and inconsistent: *"We need to be able to delete (which you added) AND also rename the rip, so it doesn't conflict."* One disc whose menu reads SEASON 4 / Disc Two is labelled `…_V7_Disc_2`, and two share one label outright so the second landed marked — none of it litter, all of it a good rip with a wrong name, and the panel had one button that threw them away. `POST /api/leftovers` now carries a `command` of `"delete"` or `"rename"` on ONE route (both verbs take the same target, answer with the same remaining list, and share the four source rules, now a shared helper rather than a copy). A rename REFUSES a destination that already exists — the reason to rename is a collision, so resolving one by overwriting an 8 GB ISO with another would be unrecoverable, and `finaliseDestination` makes the same promise from the rip side; the two-syscall window is stated rather than hidden, because Node has no `RENAME_NOREPLACE`. A file-shaped rip keeps its `.iso`, appended from the shape on disk. The new name must be one path segment and need NOT be a leftover's — dropping the `(rip-deck-duplicate-…)` marker is the point. ⚠️ **`classifyLeftover` could not see a DVD duplicate at all**: it tested `endsWith(")")`, true of every Blu-ray duplicate and false of every DVD one, since the marker goes BEFORE the extension — so the panel that resolves collisions was blind to the exact folder this was asked for. **Refines** the same week's DVD-is-an-ISO decision |
| 2026-08-26 | [A failed bay's chip is red, and a cancelled one is neither red nor green](2026-08-26-a-failed-bay-chip-is-red-not-the-finished-green.md) — the owner, about the live rack: *"When Rip Deck fails, the color of the badge is green, not red."* One `Set` caused it — `LATCHED_STATES` held `completed`, `failed` and `cancelled` together and every member returned the SUCCESS chip, so `03 failed` was painted the same green as `02 done` and only the word differed. `failed` now takes the danger chip, and its branch runs BEFORE the verdict branch: a failed bay keeps an actionable verdict as its WORD (`disc_scratched`) but never drops to that verdict's amber — "go clean the disc" and "there is no backup" are two facts and the second is louder. `cancelled` goes neutral: the owner stopped it, so it is not an alarm, and it made no backup, so it is not green. ⚠️ **No fixture reproduced this** — `mockDataSource`'s one failed bay carries `disc_scratched` and took the amber branch, so the green case had never been rendered; a new `DriveRail` story covers the whole row. The tests assert the CLASS, not the text, because the old code already printed the right word |
| 2026-08-26 | [A DVD backup is one ISO file, not a directory](2026-08-26-a-dvd-backup-is-one-iso-file-not-a-directory.md) — slot 6 reached `MSG:5070 "Backup done"` and rip-deck reported `empty_output`. The output was an 8,203,894,784-byte file that `file(1)` reads as `UDF filesystem data 'TEENAGE_MUTANT_NINJA_TURTLE_V6'` and which loop-mounts with an intact `VIDEO_TS` — a perfect rip. `makemkvcon backup` emits a **directory** for Blu-ray/UHD and a single decrypted **ISO file** for DVD, says nothing about which, and rip-deck was written against the only shape it had ever seen. `verifyBackupStructure` now accepts either, keying an image on the ISO9660 `CD001` signature at byte 32769 (no extension exists to key on, and "big file" would bless a truncated one) and holding both to the same size floor; a file-shaped rip publishes as `.iso`, with any collision marker BEFORE the extension; and `applyOutputOwnership` returns early on a file rather than throwing `ENOTDIR` into `failureOfChown`. ⚠️ This also explains `MSG:5068` and **corrects that record's guess**: the check is not per-format — MakeMKV wants to CREATE a file at that path and a directory is in the way. **Refines** the same day's destination-folder decision |
| 2026-08-26 | [DVD decryption needs `mmgplsrv`, which is a musl binary on a glibc base](2026-08-26-dvd-decryption-needs-the-musl-mmgplsrv-loader.md) — **no DVD had ever ripped in this image, and nothing said so.** `/opt/makemkv` is not as self-contained as the transplant comment claims: `makemkvcon` ships its own glibc loader, but DVD decryption is delegated to `mmgplsrv`, a separate GPL binary that jlesage's Alpine build links against **musl**. Its `PT_INTERP` is `/lib/ld-musl-x86_64.so.1`, which does not exist on the Debian runtime base, so it could never start — `MSG:4041 "Failed to execute external program 'mmgplsrv'"` → `MSG:5069 Backup failed`. Blu-ray and UHD never touch it (AACS and BD+ live inside `makemkvcon`), which is why every rip the tower had ever completed worked and the defect stayed invisible until the owner's first DVD batch. Three files come across — the musl loader at its exact `PT_INTERP` path, plus `libstdc++`/`libgcc_s` in their own prefix behind `/etc/ld-musl-x86_64.path`. ⚠️ **Never** put Alpine's `libstdc++.so.6` in `/usr/lib` — it would shadow Debian's for `node`. New build-time assertion, because the `makemkvcon` smoke test never spawns the helper and could not catch this. Proven same-disc/same-drive: unpatched → 0 bytes; patched → `MSG:1011 "Using LibreDrive mode"` and 607 MB in ~110 s. ⚠️ The `READ OF SCRAMBLED SECTOR` kernel errors were a SYMPTOM of this, not a drive fault |
| 2026-08-26 | [`makemkvcon backup` owns creating its destination folder — we only name it](2026-08-26-makemkvcon-owns-creating-its-own-destination-folder.md) — four DVDs started ripping and all four failed with `empty_output`, which names no cause. The robot log did: `MSG:5068 "Folder … already contains a backup, please choose another folder"` → `MSG:5069 Backup failed` → **exit 0**, nothing written. `destination.ts:201` had created that folder with `mkdir` a moment earlier, and MakeMKV refuses a destination that exists — even an empty one it would have created itself. "Already contains a backup" is a misleading message; it means "this path is taken". `prepareDestination` now only NAMES the leaf (and is synchronous, because it does no I/O), and a failed rip reports `incompletePath` only when the directory is really there. ⚠️ makemkv path only — cyanrip is spawned with that directory as its `cwd` and creates its own; the two disagree on purpose. Proven by A/B on the live tower, same disc and drive, one isolated container each: pre-created → MSG:5068; absent → `PRGV` climbing |
| 2026-08-26 | [A disc's name comes from udev's volume label, before `makemkvcon` is asked](2026-08-26-a-discs-name-comes-from-udev-before-makemkvcon.md) — 8 DVDs in, all 8 nameless, the owner asked to type a title for every one: *"Do I have to type a name?"* Their labels were in `/run/udev/data` the whole time. Name precedence is now operator → **disc's own `ID_FS_LABEL`** → `makemkvcon info`, as a pure `chooseDiscNameSource` (the I/O path has no test seam). `decideDiscType` carries the label out on its `rip` decision — it already read that record to route the disc, so **no second read is added**. Two measured facts justify it: `info` ignores `--noscan` and walks the whole bus (like `backup`, 2026-07-25 — the comment claiming otherwise was wrong), so one wedged drive taxes eight good bays; and it outran its 120 s timeout on all of them. ⚠️ `ID_FS_LABEL`, never `ID_FS_VOLUME_ID` — the ISO9660 field caps at 32 chars and truncates `…Turtles_V7_Disc_2` to `…Turtles_V`, losing the disc number. A blank label is not a name. **Refines** the same day's identify-timeout decision |
| 2026-08-26 | [The health-verdict gate counts its own corpus, and what it publishes is always `suspected`](2026-08-26-the-health-verdict-gate-counts-its-own-corpus.md) — every bay card carried a four-sentence note about rip-deck's own build state, ending "tuning them needs about 30 real rips of data (there are 3)": a hand-typed count that was wrong from the first rip after it was written, repeated verbatim on nine bays. It is gone, and so is the `unknown` verdict's message — the rip's own outcome sentence (`empty_output …`, the path a partial rip was kept at) survives, because that is a fact about the RIP and not about the engine. `IS_HEALTH_VERDICT_PUBLISHED` is no longer a boolean somebody flips: `health/corpus.ts` counts `*.features.json` in the state directory and looks for at least one job that went badly, and the gate latches open at 30 with one — refreshed at daemon start and after each rip seals its vector, so every read is synchronous. An open gate lets `towerFeed` show the engine's real verdict, read back from `<uuid>.verdict.json`. ⚠️ **Everything it publishes is forced to `suspected`** by `hedged()`: the gate opens on FILE COUNTS, so the thresholds behind that verdict are still invented, and `isAnnounceable` asks for exactly one property. The third original condition — that somebody actually tuned the thresholds — is on the honour system, by the owner's decision |
| 2026-08-26 | [A `starting` bay does not refuse the Tower off press, and an identify timeout must resolve](2026-08-26-a-starting-bay-does-not-refuse-the-tower-off-press.md) — 8 DVDs in, nothing ripping, five bays `starting` for 75 minutes, and the Tower off button refused: the one control that clears a wedged USB bus was held shut by the wedge. Root cause is `identifyDisc`'s timeout, which sent `SIGKILL` and then waited for `close` — but a `makemkvcon` talking to a drive in SCSI error recovery is in uninterruptible sleep, where a signal is queued and never delivered, so the read never returned and `starting` had **no upper bound**. The timeout now RESOLVES on its own authority (keeping the events it had, `unref()`ing the child); and `power_off` is refused by `ripping` alone, because `applyRipStarted` flips the phase *before* the ripper spawns, so a `starting` bay has written nothing and a power cut loses nothing. New `skipped_starting` result kind — warned, not silent. ⚠️ Tray commands still refuse `starting`; the exception is mains only. Also fixes a fixture that made the existing power_off refusal test pass for the wrong reason (it never called `onRipStarted`, so it was asserting on a `starting` bay while claiming to test a ripping one). **Refines** the 2026-07-30 tower-off decision |
| 2026-08-25 | [The disc-type mark is right-aligned against the percentage, and the slot pill is capitalised](2026-08-25-the-disc-mark-is-right-aligned-against-the-percentage.md) — the four marks are four different WIDTHS (69.45px for 4K down to 24px for the drawn fallback), so drawing one in front of the disc name gave a stack of bay cards nine different title left-edges. The mark moves to the right, immediately left of the percentage, and the percentage reserves a fixed three-digit column (`min-w-[5.5ch] text-right`, sized for `100.0%` rather than the `19.0%` a card usually shows). Marks now share a right edge and titles share a start; a rip crossing 99.9% → 100.0% no longer steps the mark sideways. `min-w-`, not `w-`, because `percentText` is also `done` / `read errors` / `in progress`. The slot pill reads **`Slot 7`** on all three cards that draw one, matching the progress bar's accessible name; "slot" inside a SENTENCE (`TrayControls`, `LoadedDiscsBanner`, the MQTT announcement) is untouched |
| 2026-08-25 | [The disc-type mark is the real disc logo, not an emoji](2026-08-25-the-disc-type-mark-is-the-real-logo-not-an-emoji.md) — `format.kindIcon` returned 🔷 for `bluray` and 🟦 for `uhd`: a blue diamond beside a blue square, at 17px, for the one pair `armView.toArmKind` refuses to conflate. `DiscKindLogo` draws the marks printed on the discs themselves, as inline SVG so the dashboard needs no route to the internet. The CD and DVD wordmarks take `currentColor` because that is how they are printed and the source art was white-only (invisible in light mode); Blu-ray and Ultra HD Blu-ray keep their blue, which is how they are recognised — ⚠️ do not flatten all four. `data` and unknown kinds get a drawn disc, because a data disc carries whichever mark its blank was pressed with. The mark carries a `<title>` rather than `aria-hidden`: `discTypeText` is null on every ledger-adopted bay, so it is the only place those cards name the type at all |
| 2026-08-20 | ["Mark as taken out" trusts the operator over the drive's media reading](2026-08-20-mark-as-taken-out-trusts-the-operator-over-the-drive.md) — `clear_loaded` used to skip every PRESENT bay so as not to "claim a disc anyone can see is gone". Nobody can see it: these drives keep reporting their disc long after the tray opens, and the reminder's own sentence ("Press Open trays") means the tower is on, so the skip was not an edge case but every case — the owner pressed the button repeatedly against a `cleared: 0` nobody rendered. A present latched bay is now marked **`isLoadedDismissed`** and drops out of the summary: display-only (the latch, the fingerprint and the ledger record are untouched, so nothing re-rips), cleared by `start`/`rearm`/`quarantine`/the outcome latch so it dies with its disc, and persisted on `BayLedgerRecord` so a deploy does not resurrect the reminder. The button also renders the daemon's answer whenever the banner is still on screen. **Supersedes** the present-bay clause of the 2026-07-30 ledger-rebuild decision |
| 2026-08-20 | [An operator command publishes the bay table at once, not at the next tick](2026-08-20-an-operator-command-publishes-the-bay-table-at-once.md) — the ⏏ toggle stands on `last_tray_command`, `/json` published it only on `onTickComplete` (5 s), and the dashboard refetches in the `finally` of its own tray POST — always inside that window. So Open trays opened a drawer, the toggle still offered `open_bay`, and pressing it re-opened an open tray while `Close trays` (which reads the in-memory table) worked fine. New **`onBayTableChanged`** handler, fired by `rememberTrayCommand` and `runClearLoaded`, republishes the roster synchronously before the command's response is written. A second handler on purpose: `onTickComplete` claims a poll happened, and none did |
| 2026-08-20 | [A started rip proves the drawer is shut, so it corrects the tray memory](2026-08-20-a-started-rip-proves-the-drawer-is-shut.md) - `lastTrayCommand` is written only when rip-deck moves a tray, so a drawer the operator pushed shut by hand keeps reading `open_bay` through a whole rip. `open_trays` folds that field to ask whether every finished bay is already open, so one stale value skipped the escalation's first step and opened all nine drawers on the FIRST press. `applyRipStarted` now records `close_bay`, because a drive cannot read a disc with its drawer out. Also: the refusal names the verb of the command that ran (a refused `close_trays` said "Refused to **open**"), and the opened list sorts by slot instead of ripped-then-unripped |
| 2026-08-16 | [The dashboard has a client router, and the daemon's index fallback widened with it](2026-08-16-the-dashboard-has-a-client-router.md) — `<BrowserRouter>` + a one-entry `<Routes>` table plus a catch-all redirect. `router.ts` served index HTML for exactly `/` and `/index.html` and said in so many words that a router arriving should widen it there, so it did, on the narrowest rule that works: **`/api/` and `/assets/` are reserved and never client routes; otherwise any extension-less path serves the dashboard, and a path whose last segment has a dot does not.** Those guards are the point: a missing `/assets/index-abc123.js` must fail as a 404 the browser reports, never 200 + HTML the browser tries to execute as JS, and an unshipped `/api/rips` must keep the JSON 404 a JSON client can act on. Cost: a mistyped `/jsonn` is a page now instead of a JSON 404. The old "404s in JSON, never in HTML" test split into two halves, and the file-naming one is the half that was protecting anything |
| 2026-08-14 | ⛔ **Superseded 2026-08-23 — the display name is `Rip Deck`, no hyphen.** [The display name is **Rip-Deck**, not "Rip Deck"](2026-08-14-the-display-name-is-rip-deck-not-two-words.md) — prose, UI and the HA device `name`/`manufacturer` are the identifier slug title-cased with the hyphen kept; identifiers are untouched and HA entity ids still slugify to `rip_deck`. Supersedes the prose row of the 2026-07-28 rename |
| 2026-08-09 | [**Mobile poster-tap redesign is deferred** — the always-on inline poster + Lightbox on narrow cards is enough for now; full-bleed "tap → logs" may be revisited later. Options mock kept at `docs/previews/2026-08-09-mobile-poster-tap-options.html`. PR after-shots had no art because mock fixtures leave `poster: null`](2026-08-09-mobile-poster-tap-deferred-inline-poster-is-enough.md) |
| 2026-08-09 | [Light-mode chrome uses Charcuterie tokens; poster and title stay on narrow cards](2026-08-09-light-mode-chrome-and-narrow-poster.md) — bay chrome / action buttons leave hardcoded dark hexes for `surface`/`content`/`border`/`intent-*` tokens and `@charcuterie/ui` `Button`; the poster is always shown when present (`w-16` narrow / `w-28` at `@md/bay`), and the title wraps instead of truncating behind the action row. **Closes** the half-done light-mode caveat on the 2026-08-03 first-paint decision |
| 2026-08-09 | [A blind ledger write must not wipe finished-disc records](2026-08-09-blind-ledger-write-must-not-wipe-finished-discs.md) - `persistLedger` skips empty `records` while blind even if `trayCommands` remain (that hole wiped latched discs after Larry Flynt); in-flight writes queue the latest payload instead of dropping a completion. **Refines** the 2026-07-30 ledger-rebuild decision |
| 2026-08-09 | [HA bay discovery keys on slot, not USB path](2026-08-09-ha-discovery-keys-on-slot-not-usb-path.md) - discovery `unique_id` / object id is `slot_NN` so a re-cable only rewrites `state_topic`; path-keyed discovery left tablet-dash on dead topics after `2-1.1.2` → `2-1.3`. Removed bay object ids are tombstoned. **Refines** the 2026-07-30 USB path change decision |
| 2026-08-03 | [First paint follows the OS scheme, and a header switcher owns the choice](2026-08-03-first-paint-follows-the-os-scheme.md) — the dashboard now **follows the OS light/dark preference by default** (`mode` defaults to `system`) and lets the operator override it: `@charcuterie/ui`'s **`ColorSchemeSwitcher`** (wrapped as `SchemeSwitcher.tsx`, inline-SVG sun/moon/monitor, no new icon dep) sits in the header and cycles **light → dark → system**, persisting to `localStorage` (`charcuterie-scheme`) and applying `data-scheme` on `<html>`. `index.html`'s first-paint rule is replaced by `@charcuterie/tokens`' **`buildFirstPaintScript(daylight)`** pasted verbatim — it sets `data-scheme` before any stylesheet parses and **branches the `var()` fallback hex on the resolved scheme** (`#131822` dark / `#F5F7FA` light), so there is no flash of the wrong theme on reload. `firstPaintColour.test.ts` now asserts the generator's own string and the absence of any pinned `data-scheme` in the tag; `biome.json` excludes `index.html` so it cannot reformat the gated paste. `@charcuterie/logic@^1.1.0` added. Verified live (four states screenshotted); 1285 tests green. **Supersedes** the pinned-`data-scheme="dark"` stance in the old `index.html` |
| 2026-08-02 | [Rip Deck builds on push and deploys like the fleet, not by hand](2026-08-02-rip-deck-builds-on-push-like-the-fleet.md) — CI gains a `docker-deploy` job mirroring gallery-downloader: a push to `main` builds the image once and pushes `ghcr.io/sawtaytoes/rip-deck:{latest,<sha>,<ref_name>,<semver>}`; the app consumes the moving `:latest` with `pull_policy: always` and TrueNAS force-pulls on redeploy. Hand-built `docker build`/`push` on Tower is retired. `package.json` `version` is the semver source of truth (bumped `0.1.0` → **1.2.6**); it never regresses to 1.2.1. Also the merge landing point for the `@charcuterie` UI (feat/charcuterie-1.0.0 / PR #13) onto main, daemon fixes intact |
| 2026-07-31 | [A tower power-cycle holds a finished disc from the bay's own memory](2026-07-31-power-cycle-holds-a-finished-disc-from-bay-memory.md) — the 1.2.4 cold-power-on guard (keyed on `hasSettledEmpty`) **re-ripped a completed Desk Set Blu-ray live** when only the tower's USB power cycled: the daemon never restarted (so `adoptBayAtStartup` never ran), the in-memory `ledger` is a startup snapshot frozen before the disc was ripped, and the ~10 s spin-up re-armed the `done` bay — which hard-sets `hasSettledEmpty: true` and erases the on-disk record. Fix: a new `BayState.lastFinished` (`BayLedgerRecord`), set on terminal latch and **carried across the re-arm**; the re-adopt gate holds a present disc whose **size matches** it (or the ledger), independent of the poisoned flag. Re-arm untouched (the [12V ADR](2026-07-28-empty-tray-clears-a-terminal-verdict.md) stands). A genuinely different disc (different size) still rips; a same-exact-size reinsert is held (press Rip). 1280 tests. **Refines** the cold-power-on decision below |
| 2026-07-31 | [A disc present when the tower powers on is adopted, not re-ripped](2026-07-31-a-disc-present-at-power-on-is-adopted-not-ripped.md) — on a cold power-on the USB bus enumerates a drive a moment before its disc is readable, so the first sighting looks **empty**; the disc that appears a poll later then read as a fresh insert and **re-ripped a finished disc** (measured: an already-backed-up Soylent Green UHD). Fix: `BayState.hasSettledEmpty` — a rip starts only from a bay whose tray was confirmed empty for `settleEmptyObservations` (3) polls first; a disc that appears before then is routed back through `adoptBayAtStartup` (held / ledger-recognised / owner's-decision rip). Preserves "load the tower then start → rips" and "new disc rips"; only stops re-ripping a **finished** disc across a power cycle. Fail-closed (holds → press Rip). 1020 tests. **Refines** the ledger-rebuild decision below |
| 2026-07-30 | [The loaded-discs reminder is rebuilt from the on-disk ledger, not held in MQTT](2026-07-30-loaded-discs-rebuild-from-the-bay-ledger.md) — the "N discs still in the tower" fact is rip-deck's own state, rebuilt on startup from the **bay ledger** (`bays.json`) for any drive the probe did not answer (`phantomLoadedBays`), so it survives a restart against a dark tower without leaning on the broker's retained copy. Phantoms are **display-only** — `isDrivePresent: false`, never in the `bays` map — so they cannot re-rip or auto-eject; a returning disc is re-adopted fail-closed (HOLD, not re-rip). `/json` and MQTT read the ONE summary (`main.ts` folds `getLoadedDiscs()` into the snapshot; `buildTowerView` prefers it) — fixing the blank Web UI banner after a tower-off restart. `isBlind` replaces "empty means unknown": a **readable** empty ledger publishes a real all-clear, only a truly blind daemon withholds. Adds a **`clear_loaded`** command + "Mark as taken out" button (same `cmd/drive` + `/api/tray` surface). The retained topic stays as a downstream **mirror** for HA. **Refines** the tower-off decision below |
| 2026-07-30 | [The dashboard can switch the tower off, and remembers what it trapped](2026-07-30-the-dashboard-can-switch-the-tower-off.md) — a **`power_off`** command + a **Tower off** button, publishing `off` on the existing `cmd/power` topic. A running rip refuses the WHOLE press (one power lead); a bus that will not answer refuses too; loaded-but-idle discs are **warned about and the tower goes off anyway** (owner's call). No Tower ON button — Open trays already powers a dark tower. Plus the fact that has to outlive the power: a **retained `loaded` topic**, a `Discs loaded` sensor, a slate dashboard banner and an arrival/evening **phone reminder**, answered from the bay + sighting tables because a dark tower has nothing to probe. `shouldPublishLoadedDiscs` is what makes the retained topic trustworthy: **a summary that is both empty and blind is never published**. ⚠️ Fixed an HA hazard first — the `cmd/power` trigger matched the topic alone and would have turned the tower **on** when asked to switch it off |
| 2026-07-30 | [A held bay is ripped from the dashboard, not from a shell](2026-07-30-a-held-bay-is-ripped-from-the-dashboard.md) — the held card gains a **name box and one button** (empty → "Try again", typed → "Rip as this"), backed by a new **`rip_bay`** command on the existing `cmd/drive` + `POST /api/tray` surface. It overrules the latch, the fingerprint and the start counter — all guards on the POLL LOOP, and a person pressing Rip is not the poll loop — but **never the `starting`/`ripping` refusal or the governor**. An operator-supplied name is not an invented one (B3 is untouched; it is `--name` through a text box). The card used to print a CLI command it could not run, beside a ⏏ that does not un-hold on this rig. Also retires two wrong instructions: the `--name` CLI hint and *"open the tray and close it again"* |
| 2026-07-30 | [The spoken message and the written message are separate fields](2026-07-30-spoken-and-written-messages-are-separate-fields.md) — every payload HA may **speak** gains an additive `spoken_message` (`resp/drive` and `rip/event`, plus `slot` on the latter); HA speaks it and falls back to its old templates. Traced live: `job_status_announcement` never read `message` at all, so the CLI prose never reached TTS — what the owner actually heard was **`"A rip failed on 09 - Pioneer BDR-211M"`**, a drive model read out as a part number *and* the word "failed" about a `needs_attention` **hold**, which `result: "fail"` cannot distinguish. Spoken text never carries device words, counts, slot lists or drive models. **Corrects the named example in** the gaps doc |
| 2026-07-30 | [The tower's mobo USB3 port survived — the cable was the fault](2026-07-30-tower-usb3-port-survived-the-cable-was-the-fault.md) — the suspect "bottom-right rear USB3 port" left open by the 12V incident is **good**: a new **10 m active** extension (with a **power-indicator LED**) on the **PSU 5 V rail**, back on that same original port, enumerates the whole bank at SuperSpeed. Every USB fault this tower threw was a cable — undervolted, then over-volted-to-death — never the port. Tower path moved `2-1.1.2` → **`2-1.3`** (`config/drives.json` `towerRootPortPath` updated; per-drive hints self-repair). Don't re-litigate the port or propose a powered hub / second xHCI. **Refines** the 2026-07-24 wedge decision |
| 2026-07-30 | [Identify retries until the disc is READ, not until a drive answers](2026-07-30-identify-retries-until-the-disc-is-read-not-until-a-drive-answers.md) — the retry gate changes from `didDeviceRespond` to **`wasDiscRead`** (a `CINFO` block, proof the disc was opened). A **UHD disc ("SOYLENT GREEN - UHD", slot 9)** inserted mid-enumeration listed its DRV line before LibreDrive/BD+ decrypt emitted any `CINFO`, so the old gate mistook "disc not open yet" for "blank label" and latched `needs_attention` — the whole tower read as "not ripping." Now no-`CINFO` reads retry; genuinely-blank and spawn-fault stay one-shot; still fail-closed. 1195 tests green. **Refines** the 2026-07-28 single-read decision below |
| 2026-07-28 | [A single-read identify latched a flapping bus as "could not read a name"](2026-07-28-single-shot-identify-latched-a-flapping-bus.md) — `identifyDisc` now retries a read **no drive answered** (up to 3, 1.5 s apart), gated on `didDeviceRespond` so a genuinely blank label and a missing binary stay one-shot. A doubled passive USB cable flapped the whole bank (`error -71`), and one empty read latched slot 7's Soylent Green UHD as `needs_attention` — the next read seconds later got the label. The dashboard also gains a bus-wide `usb_alert` "change your USB connection" banner (its own field, not the per-bay `alerts` list, because a flap is what *holds* bays), driven from drive-presence flaps the watcher already sees. `?fake=usb-flap` previews it. Unverified on the tower until the replacement cable arrives ~2026-07-30 |
| 2026-07-28 | [The project is renamed `ripdeck` → `rip-deck` ("Rip Deck")](2026-07-28-rename-to-rip-deck.md) — the deepest rename on every axis (infra, image, npm scopes `@rip-deck/*`, env `RIP_DECK_*`, CLI `rip-deck`, MQTT base `rip-deck/tower`, nodeId `rip-deck_tower`, `/var/lib/rip-deck`, TrueNAS app, `example.com`, broker user, HA device "Rip Deck" → `sensor.rip_deck_*`). JS identifiers/JSON key are camelCase `RipDeck`/`ripDeck` (hyphen illegal); HA ids are underscore `rip_deck`. **Supersedes** the old "identifiers stay lowercase single-token `ripdeck`" README contract |
| 2026-07-28 | [Rip Deck is a TrueNAS custom (docker-compose) app, not a hand-run container](2026-07-28-rip-deck-is-a-truenas-custom-compose-app.md) — `app.create` with `custom_app: true` from `deploy/docker-compose.yaml`, image on `example.com`. The `custom-app` chart can't express `device_cgroup_rules` (block 11 / char 21, wildcard minor because `/dev/srN` reshuffles), which the optical tower needs — so raw compose is the only path. Icon is a data-URI SVG poked into `metadata.yaml`; the middleware only reloads it on a `middlewared` restart |
| 2026-07-28 | [The deployed image runs compiled JS on node, never tsx](2026-07-28-compiled-js-on-node-not-tsx.md) — multi-stage build; esbuild bundles the daemon (tsc can't, the source imports `.ts` extensions); runtime ships only the bundle + dashboard + config, no node_modules/tsx. 875→596 MB, ~300 MB less RAM. Fixed `isWatchInvocation` keying on `.ts` (compiled `cli.js` had exited 0, tower unwatched) and pinned `RIP_DECK_WEB_DIST` |
| 2026-07-28 | [An empty tray clears a terminal verdict, even across a flapping drive](2026-07-28-empty-tray-clears-a-terminal-verdict.md) — a bay tile reflects what is in the drive *now*; verdicts live in the rip record, not the tile. `applyBayDecision` now **holds** `emptyObservationCount` on an off-bus poll instead of resetting it, so a drive flapping off the bus between two confirmed-empty reads still re-arms. Slot 5 latched `failed` with no disc for hours during the 12V incident because the flap kept zeroing the debounce. Re-arm still only fires on a present-and-empty poll, so a `done` disc still in the tray never re-rips |
| 2026-07-27 | [With nothing to be selective about, the bulk tray buttons open all and close all](2026-07-27-bulk-tray-buttons-fall-back-to-open-all.md) — ▲ opens every loaded bay when **no** bay is latched completed/failed, ▼ closes every present bay; the tower-wide `isAnyBayFinished` is folded by the caller so the per-bay decision stays pure. On an idle tower every bay answered `skipped_not_finished` and no drawer moved, which reads as a broken button. **The `starting`/`ripping` refusal is untouched and still first**, and ▲ still skips an empty bay |
| 2026-07-27 | [What rip-deck did to the drawer beats a guess about the door](2026-07-27-tray-memory-beats-disc-presence.md) — `lastTrayCommand` is authoritative for tray position and `hasMedia` is only the fallback. Measured on the rack: `open_completed` opened slots 7-9 and **all three went on reporting a disc**, so `close_open` refused to close exactly the bays it had just opened and the toggle could never send its second press. Do not "simplify" this back to trusting disc presence |
| 2026-07-27 | [Rips mount at `/media/Disc-Rips`, and only a `-v` *source* must be a host path](2026-07-27-rips-mount-at-media-disc-rips.md) — matches how mux-magic mounts all thirteen of its datasets; the host path appears only as a `-v` source, `RIP_DECK_DEST_INNER` stays unset, and the audit for "code that assumes its own path is the host's" came back clean because the isolation args are **opaque** |
| 2026-07-27 | [ARM is deleted, not just disabled](2026-07-27-arm-is-deleted-not-just-disabled.md) — both TrueNAS apps and NPM proxy hosts 64/65 removed after disabling and re-confirming each by domain name; **App config datasets and the viewer's git repo are kept**, because `arm.db` is the historical rip record and `packages/web` was harvested from that repo |
| 2026-07-26 | [Rip Deck's own dashboard may call Rip Deck's own daemon over HTTP](2026-07-26-the-dashboard-may-call-its-own-daemon-over-http.md) — the "services talk over MQTT" rule governs **service-to-service** integration and does not reach an app's own UI calling its own API on its own port. The dashboard shipped a tray button that only explained why it could not work. **Third time** a narrow rule was widened into a capability ban here; the refusal for a `starting`/`ripping` bay still lives in the daemon |
| 2026-07-26 | [Bay memory survives a restart, and an unaccountable disc is held](2026-07-26-bay-memory-survives-a-restart.md) — `$RIP_DECK_STATE_DIR/bays.json` + `adoptBayAtStartup`; a restart with finished discs loaded used to **re-rip every one of them**, and left the eject command nothing to read. With no ledger at all a loaded disc is HELD, not ripped; with a ledger, a disc it does not mention still rips |
| 2026-07-26 | [An operator may eject; rip-deck still never eject-loops](2026-07-26-operator-triggered-eject-over-mqtt.md) — corrects the over-widened *"rip-deck never ejects"*; tray commands on `cmd/drive` + `resp/drive`, **a `starting`/`ripping` bay is refused loudly**, no decision inside rip-deck moves a tray, and `eject` is a spawned binary rather than an in-process ioctl |
| 2026-07-26 | [The tower's auto power-off triggers on idleness, never on closed trays](2026-07-26-tower-power-off-triggers-on-idle-not-on-closed-trays.md) — implements *"no rips or drive changes in X time"* off a retained `<base>/activity` topic and **refuses to implement *"or all drives closed now"***: rip-deck never ejects, so every tray is closed during every rip (nine closed, three rips live, measured). Fails closed — missing/stale/`unavailable` state never powers anything off |
| 2026-07-26 | [`rip-deck` adopts RxJS — on the async edges, not on the pure folds](2026-07-26-rxjs-on-the-async-edges-not-the-folds.md) — closes the open alignment question; `rxjs ^7.8.2` in `@rip-deck/daemon`, streams for the poll loops and dispatch, **reducers stay pure functions of state and an injected clock**; timers come from `unrefTimers.ts` and `mergeMap` is always bounded |
| 2026-07-26 | [Each rip runs in its own container that can see one `/dev/srN`](2026-07-26-each-rip-runs-in-its-own-device-scoped-container.md) — the forced bus scan finds one drive, the index is always `disc:0`, and a wedged sibling is structurally invisible; opt-in on `RIP_DECK_RIP_ISOLATION_IMAGE`, and `verifyDiscIndex` stays even though it becomes near-tautological. **The prerequisite the auto-rip decision names** |
| 2026-07-26 | [Auto-rip every inserted disc, concurrently, routed by disc type](2026-07-26-auto-rip-every-inserted-disc-concurrently.md) — insert nine discs, get nine rips; CD → cyanrip, DVD/BD/UHD → makemkvcon `backup`. Drops the one-slot refusal; **per-rip device isolation is a prerequisite**, because `backup` re-scans the whole bus |
| 2026-07-26 | [ARM stays disabled — `rip-deck` is the ripper now](2026-07-26-arm-stays-disabled.md) — retires the "ARM must keep ripping" rule; the one-slot limit stays, now justified by rip-deck's own unproven concurrency rather than by protecting ARM *(one-slot clause superseded the same day)* |
| 2026-07-25 | [`makemkvcon backup` takes a disc index, and scans the whole bus to resolve it](2026-07-25-backup-takes-a-disc-index-and-scans-the-bus.md) — `dev:` is rejected on rip paths and `--noscan` is ignored; verify the index against MakeMKV's own DRV table, and isolate devices per rip to get E2/E3 back |
| 2026-07-25 | [`rip-deck` ships its own image with `makemkvcon` in it](2026-07-25-rip-deck-ships-its-own-makemkv.md) — closes the §3.0 blocker and open question 6; no borrowing ARM's container, one filesystem view, drives via device-cgroup-rules |
| 2026-07-24 | [The rip tower's USB wedge is explained by an active extension cable run passively](2026-07-24-active-usb-extension-aux-power-explains-the-wedge.md) — supersedes the findings-doc §8 diagnosis; keep aux power plugged in, don't buy a hub or second controller yet |
| 2026-07-24 | [Build `rip-deck` rather than adopt an off-the-shelf ripper](2026-07-24-build-rip-deck-rather-than-adopt.md) — no adoption target survived the requirements catalogue; grow it in stages beside ARM, health sidecar first |
